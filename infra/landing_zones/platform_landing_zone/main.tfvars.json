{
  "admin_object_ids": "${ADMIN_OBJECT_IDS}",
  "aks_node_pool_vm_size": "${AKS_NODE_POOL_VM_SIZE}",
  "alert_email": "${ALERT_EMAIL}",
  "application_gateway_trusted_root_certificate_base64": "${AGW_TRUSTED_ROOT_CA_BASE64}",
  "environment": "${AZURE_ENV_NAME}",
  "firewall": ${AZURE_FIREWALL},
  "location": "${AZURE_LOCATION}",
  "point_to_site_vpn": "${AZURE_VPN_GATEWAY_P2S_VPN}",
  "subscription_id": "${AZURE_SUBSCRIPTION_ID}",
  "tenant_id": "${AZURE_TENANT_ID}",
  "virtual_network_gateway": "${AZURE_VPN_GATEWAY}",
  "workload_environment": "${AZURE_WORKLOAD_ENV_NAME}"
}
